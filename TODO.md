# TODO

Рабочий бэклог пакета. Пункт, взятый в работу, уходит в спеку в
`docs/` (формат имён — `YYYY-MM-DD[N]-<тема>.md`) и вычёркивается
отсюда; сделанное живёт в CHANGELOG, а не здесь.

Из волны непрерывности ссылки
(`docs/2026-08-07[2]-link-continuity-plan/`):

- Ubuntu-число сторожа памяти: полоса перекалибрована 2026-08-08 под
  `Link?` на каждом матче (265.3–265.5 байта на матч, полоса 159…332),
  и снятое раньше ubuntu-число (229.3) к ней уже не относится —
  вычеркнуто из шапки. После первого зелёного прогона CI на ubuntu x64
  вписать новое в `benchmark/memory_guard.dart` на его место.

- `_SinkPrinterBase.prepare` (`printer.dart:442-450`) возвращает на
  место обе ссылочные переменные, но `lastState` (`:339`) оставляет
  сдвинутым: спросить у синка про кусок со стилем — значит покрасить
  этим стилем следующий `write`. Наследство волны
  `docs/2026-08-05[10]-verification-fixes-plan.md`, к ссылке отношения
  не имеет.

- Неоконченный `OSC`, который не ссылка (`ESC]0;title` без `ST`),
  съедает текст за собой ровно тем механизмом, который для ссылок
  закрыт в этой волне, и на всех трёх поверхностях сразу: срез,
  напечатанная строка и `optimize` отдают `ESC]0;titleword` там, где
  текст был `word`. Держать такой код негде — своего канала у него
  нет. Наследство — шире, чем брала эта волна.

Остальные пункты бэклога взяты в работу волной
`docs/2026-08-05[11]-backlog-wave-plan.md` и закрыты ею.
