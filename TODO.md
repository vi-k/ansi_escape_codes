# TODO

Рабочий бэклог пакета. Пункт, взятый в работу, уходит в спеку в
`docs/` (формат имён — `YYYY-MM-DD[N]-<тема>.md`) и вычёркивается
отсюда; сделанное живёт в CHANGELOG, а не здесь.

- Завести `.github/dependabot.yml` для `github-actions`: SHA-пины
  экшенов (checkout, setup-dart, upload-artifact) обновляются только
  руками — setup-dart просидел на v1.0.0 с 2021 года, пока его не
  заметило ревью.

Из отчёта верификации (`docs/2026-08-05[9]-review-verification-report.md`):

- Непрерывность ссылки: ни срез, ни напечатанная строка не умеют
  переоткрыть гиперссылку так, как переоткрывается стиль (`Link` не
  входит в `State`) — нужен заход по дизайну (находка N5).
- Алфавиты фаззеров: незакрытый OSC и второй обрезанный CSI для
  `has_agrees_with_parser_test.dart`; накрыть `ansiHasSgr` и
  `ansiRemoveSgr`, запинить пофункциональную сохранность (находка N8).
- Один цикл, пинящий имена к кубу:
  `Color256.rgb(r, g, b).color == Colors.values[16 + 36 * r + 6 * g + b]`
  (находка N7).
- Строка в known limitations про сторону удаления у SGR с переполнением
  целого и про то, что `optimize` не закрывает ссылку там, где её
  закрывает `substring` (находки N10, N11).
- CI: гонять примеры циклом по `example/*.dart`, подложить порог под
  покрытие; `lengthWithoutEscapeCodes` без сборки очищенной строки
  (находка N12).
- Механическая проверка замыкания точек входа: тесты `entry_point_*` —
  рукописные списки имён, и ровно такая дыра (публичная сигнатура
  называет неэкспортированный тип) сквозь них прошла; скрипт или тест,
  обходящий публичные сигнатуры каждой точки входа и требующий, чтобы
  каждый названный там тип был экспортирован, поймал бы следующую.
