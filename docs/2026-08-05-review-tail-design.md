# Хвост ревью: M6, NoStyle, entry points, CI, гигиена — дизайн

ansi_escape_codes, 2026-08-05. Написано до кода, по итогам
`2026-08-04-project-review.md` (после этого захода —
`docs/review/2026-08-04-1-project-review.md`): пункты #8 (M6), #10
(L1–L3), #11 (L7/L8), #13 (L9/L10), #15 (гигиена) плана действий.
Закрывает весь остаток ревью, кроме генератора таблиц (#12). Целевой
релиз — та же неопубликованная 4.0.0; публикация — отдельное решение
пользователя.

Решения пользователя, зафиксированные до этой спеки:

- M6 — **док-контракт**, поведение `==` не меняется;
- L3 — **чиним поведение** Printer, не док;
- TODO.md — **не удалять**, наполнить назначением;
- отчёт ревью — перенести в `docs/review/`;
- слияние — локальным `merge --no-ff` в main, **без PR**.

## Цели и критерии готовности

Ветка `fix/review-tail` от main. Один пункт — один коммит,
порядок: **M6 → L1+L2 → L3 → L7 → L8 → L9 → L10 → #15**.
Conventional-префиксы; версия не трогается; CHANGELOG-строки — в
секции невыпущенной 4.0.0 (Fixed для L1/L2/L3/L7, Added для L8).

Фикс считается готовым, когда: существующие тесты зелёные (единственное
намеренное касание существующего тест-файла — удаление `print('')` в
#15); `dart analyze --fatal-infos` и `dart format` чистые; red-green
для поведенческих фиксов (L1, L2, L3, L7, L8) зафиксирован.

## Раздел 1 — фиксы

### M6 — контракт равенства по видимой поверхности

Поведение не меняется. Dartdoc `State.==` фиксирует: равенство — по
видимой поверхности; история раскрутки `Stack` не сравнивается; равные
стеки могут разойтись после одинакового reset'а. У `Stack` — короткая
отсылка к контракту. Тест-пин закрепляет оба факта из ревью:
`underline.doublyUnderline == doublyUnderline` — true, и расхождение
после одинакового `resetUnderline` — документированное поведение.

### L1 — операции без изменений возвращают тот же state

`state.dart:21-25` уже обещает «нечего менять → тот же state»; `Style`
это обещание нарушает: операция строит новый объект даже без изменений,
и `NoStyle().resetItalic` вырождается в пишущий `Style`. Фикс на уровне
`Style`: операция, не меняющая ничего, возвращает `this` — `NoStyle`
чинится автоматически (он и есть Style без изменений). Точная форма
(identical-проверки в местах, где сейчас строится копия) — в плане по
фактическому style.dart. CHANGELOG Fixed.

### L2 — переход между одинаковыми поверхностями пуст

`NoStyle().transitTo(Style.terminalColors)` отдаёт `\x1B[0m` при
идентичных поверхностях — виноват шорткат с NoStyle-термом в `==`
(state.dart:230-237). Фикс — локально в `transitTo`: переход между
равными по поверхности состояниями — `''`. Сам `==` не меняется — его
поведение фиксируют M6 и существующий пин `NoStyle != terminalColors`
в `style_state_test.dart`. CHANGELOG Fixed.

### L3 — Printer с NoStyle не пишет ничего своего

`printer.dart` начинает строку безусловным reset'ом. С
`defaultStyle: NoStyle()` строка уходит как есть: ни reset'а, ни
стиля. Параметр `ansiCodesEnabled` **остаётся** — ручки ортогональны:
NoStyle значит «не навязывай свой стиль» (чужие коды в тексте проходят
насквозь), `ansiCodesEnabled: false` значит «вычисти чужие коды»
(выход — plain text). В том же коммите dartdoc Printer явно
противопоставляет пару. CHANGELOG Fixed.

### L7 — entry points: недостающие экспорты и тесты на каждую точку

`lib/style.dart` возвращает типы, которые из него нельзя именовать
(`Sgr.contains(ControlFunctionsSGR)` доступен, `ControlFunctionsSGR` —
undefined): добираем недостающие экспорты до замкнутости точки.
Экспорт-тест на **каждую** из шести точек по паттерну
`exports_test.dart` (сейчас покрыта только зонтичная): каждый
публичный тип, достижимый из точки, из неё же именуем. Судьба
дублирующего `parsing.dart` не решается — только тест замкнутости.
CHANGELOG Fixed (недостающие экспорты).

### L8 — спящий API: `intensityStyle` и тест `EscCommon`

`IntensityStyle` публичен, но недостижим — единственная парная
характеристика без геттера. Добавляем геттер `intensityStyle`
(симметрия с остальными). `EscCommon` (флагман CHANGELOG 4.0.0, ноль
ссылок в тестах) получает первый тест — исчерпывающий `switch` по
нему. CHANGELOG Added (геттер).

### L9 — CI

`.github/workflows/dart.yml`: `dart doc --dry-run` (упаковочный баг с
dartdoc уже случался — 05bbe03); сбор покрытия; исполнение примеров, а
не только их анализ; SHA-пиннинг `actions/checkout` (setup-dart уже
запинен); `permissions:` для `GITHUB_TOKEN`.

### L10 — pubspec

`repository:` и `issue_tracker:` вместо одного `homepage:`.

### #15 — гигиена

- `TODO.md` — наполнить назначением (рабочий бэклог мейнтейнера;
  пункт, взятый в работу, уходит в спеку в `docs/`), ссылка из
  `docs/2026-08-02-insert-before-after-design.md:4` остаётся валидной;
- `SPEC.md` — уже сделан: перенесён реорганизацией 2026-08-04 в
  `docs/2026-07-29-fix-spec-v3.1.2.md` (коммит 9836c89), не в скоупе;
- `print('')` из `test/ansi_constants_test.dart:60` — убрать;
- `.claude/` — в `.gitignore`;
- опечатки приватных имён в `stack.dart`: `intencityStack` →
  `intensityStack`, `scripStack` → `scriptStack` (и производные);
- **новая схема имён доков** (решение пользователя): порядковый номер
  дня после даты — `YYYY-MM-DD-N-<topic>.md`, единообразно для всех,
  включая единственный док дня. Переименование по фактической
  хронологии первого коммита:
  `2026-07-29-1-fix-spec-v3.1.2`, `2026-08-02-1-insert-before-after-design`,
  `2026-08-04`: `-1-project-review` (уезжает в `docs/review/`),
  `-2-perf-fixes-design`, `-3-perf-fixes-plan`,
  `-4-handoff-correctness-fixes`, `-5-correctness-fixes-design`,
  `-6-correctness-fixes-plan`; `2026-08-05`: `-1-sgr-classifier-design`,
  `-2-sgr-classifier-plan`, `-3-review-tail-design` (этот документ;
  план захода родится сразу как `-4-review-tail-plan`). Переименование
  через `git mv`, перекрёстные ссылки в доках обновить
  (`grep -rn "docs/2026" docs/ README.md CHANGELOG.md` — свериться, что
  битых не осталось);
- отчёт ревью — в `docs/review/2026-08-04-1-project-review.md`; ссылки
  на него в живых доках обновить.

## Раздел 2 — тесты и порядок

Red-green по каждому поведенческому фиксу, новые тест-файлы (существующие
не редактируются, кроме оговорённого `print('')`):

- **M6**: `test/state_equality_contract_test.dart` — пины контракта
  (зелёные сразу; фиксируют документированное);
- **L1/L2**: `test/no_style_test.dart` — `NoStyle().resetItalic`
  идентичен исходному state и пишет `''`; `identical(style.resetItalic,
  style)` для style без italic; `NoStyle().transitTo(terminalColors)`
  → `''`; переходы между равными поверхностями пусты — RED на текущем
  коде;
- **L3**: там же или рядом — `Printer(defaultStyle: NoStyle())` на
  строке с кодами и без: вывод байт-в-байт равен входу; с
  `ansiCodesEnabled: false` — plain text (пин ортогональности) — RED;
- **L7**: тест замкнутости на каждую точку — минимальный код,
  именующий каждый достижимый тип; RED на `ControlFunctionsSGR` из
  `style.dart`. Раскладку (файл на точку или префиксные импорты в
  одном) решит план;
- **L8**: `test/esc_common_test.dart` + геттер-тест в новом файле L8;
- **L9/L10/#15**: механика без red-green; CI проверяется прогоном
  workflow-команд локально, где воспроизводимо (`dart doc --dry-run`,
  исполнение примеров).

Финал захода: whole-branch-ревью, затем локальный
`git merge --no-ff fix/review-tail` в main и пуш; ветка удаляется.
После слияния из очереди ревью остаётся только генератор таблиц (#12).
