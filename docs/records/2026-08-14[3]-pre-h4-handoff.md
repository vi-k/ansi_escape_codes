# Состояние документа

- **Тип:** отработавший handoff перед волной H4.
- **Статус:** архивирован 2026-08-14 после закрытия H4.
- **Актуальность:** исторический снимок состояния на 2026-08-13.
- **Чему не верить:** H4 ниже назван открытым, число тестов и вершина
  `main` устарели. Текущее состояние — в `docs/handoff.md`.

# Handoff — состояние пакета и что делать дальше

ansi_escape_codes, 2026-08-13. Передача контекста для новой сессии.

**Коротко:** независимое ревью в семь заходов нашло **десять дефектов
уровня High и выше, все в обратной сборке** — там, куда зелёные ворота не
смотрят. **Шесть закрыты и влиты** (`d0723e0` — пять из них, `f1a927d` —
`H9`); седьмым закрытием ушла `M5` уровня Medium. **Четыре High
открыты**, и три из них — потеря или искажение пользовательских данных.
**Публикацию 4.0.0 до их разбора стоит придержать.**

**Где что лежит:** `AGENTS.md` в корне — правила работы над пакетом (их
читают оба инструмента: Claude Code через `CLAUDE.md`, Codex —
напрямую); этот файл — состояние пакета **и всё, что нашли или предлагают
агенты**; `docs/architecture.md` — как устроен код; `docs/backlog.md` —
**список владельца**, агенты в него не пишут; `docs/records/` — записи
волн, у каждой в шапке сказано, что с ней стало.

## Состояние репозитория

- **Всё влито в `main` и запушено; веток в работе нет.** Последняя волна
  кончается мержем `f1a927d`, поверх него на `main` лежит только
  документация — этот файл. Обе волны влиты мержем `--no-ff` после
  финального ревью всей ветки и своего зелёного CI: разбор находок
  (`d0723e0`) и переименование `Match` → `Piece` (`f1a927d`). CI на
  `main` зелёный на обеих ногах SDK — прогон `31713331242` на `dd8cd60`,
  последний код там же, что и сейчас. Отработавшие ветки
  `fix/post-review-findings` и `fix/match-rename` удалены — и локально, и
  на remote; хэш `HEAD` здесь намеренно не назван, чтобы документ не
  устаревал от собственного коммита.
- Версия **4.0.0**, на pub.dev — 3.1.2. Все правки неизданные, поэтому
  CHANGELOG правится на месте.
- Ворота на `main` зелёные, снято на дереве `a7978a4` — том самом, что
  влито мержем `f1a927d`, и том, на котором CI прошёл обе ноги SDK
  (прогон `31712886635` на ветке, `31713331242` на `main`):
  `dart format --set-exit-if-changed`, `dart analyze --fatal-infos`,
  `dart run tool/check_entry_points.dart`, `dart run tool/generate.dart`
  без диффа, **811 тестов** (было 614), `dart run
  benchmark/memory_guard.dart` — 258.2 при полосе 159…332, `dart doc
  --dry-run` 0/0, `dart pub publish --dry-run` 0 предупреждений.
  **`generate.dart` проверяется не `git diff`, когда дерево грязное:**
  диффом там видно всю несохранённую работу, а не работу генератора;
  снимать надо контрольные суммы `lib/` до и после запуска.
- Покрытие `lib/` — **77.3%** (1941 из 2510) при пороге CI 75%; без
  генерируемого `style_colors.dart` — 97.0%. Мерялось на `d0723e0`;
  переименование строк не добавляло и не убирало, но само число с тех пор
  не переснималось.
- На remote по-прежнему висят две ветки dependabot (`actions/checkout`,
  `actions/upload-artifact` до 7.0.1). Это CI-экшены, в публикуемый пакет
  не входят, к публикации отношения не имеют.

## Что сделали две волны

Отчёт ревью — `docs/records/2026-08-13[5]-independent-review.md`. Семь
независимых заходов: шесть агентов по областям плюс Codex; каждое
утверждение перепроверено пробником на живом коде. Разбор инварианта
обратной сборки — `docs/records/2026-08-13[6]`. Отработавший handoff
первой волны — `docs/records/2026-08-13[7]`. Спека и план переименования
— `2026-08-13[8]` и `[9]`.

| закрыто | коммит |
|---|---|
| **H3** `link`/`linkBel`/`Link` пропускали управляющие байты из url в терминал | `835b792`, `f7ba4c1` |
| **C1** `Stack` квадратичен по времени и памяти | `6a0a60d` |
| **H2** незавершённый код глотал текст в четырёх выходах | `cbec9d0` |
| **H10** сужение `Stack.underlineColor` не значилось ломающим | `5b05f05` |
| **M5** четыре шапки записей звали влитые волны нереализованными | `718fbd6` |
| **H5** открытый срез терял выжившую половину парного сброса | `a63cc4d` |
| **H9** `Match` молча перекрывал `dart:core.Match` | `a7978a4` |

Решения владельца, принятые за эти две волны и записанные:

- **`link` кодирует C0 и DEL, но не C1**, и не через `Uri.encodeFull` —
  тот экранирует сам `%` и портит уже закодированный адрес. `Link`
  ради этого перестал быть `const`: в const-инициализаторе нельзя ни
  вызвать функцию, ни спросить `contains`.
- **Инвариант обратной сборки:** что `removeAll` называет текстом, то и
  выходит из `optimize`, `substring` и принтеров. Разбор вариантов —
  `docs/records/2026-08-13[6]`. Цена названа там же: у оборванного `CSI`
  модель пакета и терминал расходятся уже на входе, и вывод встал на
  сторону модели.
- **Открытый срез не устанавливает коды, стоящие на разрезе** — замысел
  февраля 2025 сохранён; починена только выжившая половина парного
  сброса.
- **Имя куска — `Piece`, коллекция — `Pieces`, геттер — `parser.pieces`**,
  и переименование доведено до приватных имён и файлов. Моста
  `typedef Match<S> = Piece<S>` нет намеренно: он вернул бы то самое
  перекрытие. Разбор — `docs/records/2026-08-13[8]`.

## Найдено волнами

Здесь лежат находки и предложения агентов, а не список владельца:
`docs/backlog.md` его, и туда попадает только то, что он сам решит взять.

Подробности каждой, с замерами и ссылками на строки, — в отчёте
`docs/records/2026-08-13[5]`. **Клетки «сейчас» в отчёте устаревают по
мере разбора; что закрыто — в таблице выше.**

### Открыто, High

- **H4. Вставка глотает хвост строки.** `_insert` пишет вставляемый
  текст дословно, ни разу не спросив, чем тот кончается, хотя разбор
  этого текста у него уже в руках. `Parser('abcdef').insertBefore(3,
  '\x1B]0;t')` даёт plain-текст `abc` — `def` проглочен целиком;
  `insertBefore(3, '\x1B[31')` даёт `abcef`, где `d` не потерян, а стал
  финальным байтом команды `VPA`. **Достижимо композицией двух публичных
  API:** `substring(close: false)` документированно оставляет
  придержанное открытие, и такой срез, вставленный куда-то, портит
  данные. Фаззинг отчёта: из 98 696 вставок 9 033 изменили текст, и все
  9 033 — ровно там, где вставляемый текст кончается внутри
  незавершённой последовательности. Ссылочный случай (`OSC 8`) чинится
  **случайно** — `_linkBack` пишет `linkClose`, начинающийся с `ESC`, —
  а заголовку окна (`OSC 0`) так не везёт; эта асимметрия и делает
  находку дефектом, а не «мусор на входе — мусор на выходе». Проверено
  пробником на влитом `d0723e0`: воспроизводится. Соседняя правка `H2`
  его не трогает — она про код, пришедший из **входа**, а этот про код,
  пришедший из **вставки**. **Пропадал из прошлого handoff'а**: не
  значился ни закрытым, ни открытым, поймало финальное ревью ветки.
- **H6. Немоделируемые функции `SGR` молча удаляются** всеми выходами.
  `\x1B[11m` (альт. шрифт), `\x1B[20m` (фрактура), `\x1B[26m`/`\x1B[50m`,
  `\x1B[60m`–`\x1B[65m` исчезают целиком; `\x1B[1;11;31m` теряет `11`,
  сохраняя две другие функции; `\x1B[4:3m` (curly underline) становится
  `\x1B[4m`. Причина: `Sgr` — единственная сущность, которую не копируют,
  а пересобирают из `State`, поэтому модель работает фильтром. Спорит с
  позицией `UnrecognizedEscapeCode`. Рядом: параметр, не влезающий в
  `int.parse`, наоборот пропускается дословно как `CsiUnknown` — две
  противоположные обработки одного «мы этого не понимаем».
- **H7. `Stack` на reset снимает уровень вместо очистки поля.**
  `Styles.red('\x1B[32ma\x1B[33mb\x1B[39mc')` рисует `c` зелёным, а
  `Printer(defaultStyle: Styles.red)` — красным: два публичных API
  расходятся на одном входе, потому что `Style.call` строит
  `StackedPrinter`. Поведение намеренное и закреплено тестами **только в
  прощающую сторону**; обратная — где текст честно открыл два уровня и
  закрыл один — не покрыта ни тестом, ни дартдоком. Требует решения
  владельца: сузить область `Stack` либо задокументировать и завести тест
  на провальное направление.
- **H8. `ESC 7`/`ESC 8` не переживают границу строки в принтере.** Слот
  сохранения живёт в `_ParserIterator`, который принтер пересоздаёт на
  каждую строку. Документ `'\x1B[31m\n\x1B8mid\ntail'`, прочитанный
  целиком, даёт `mid` красным и `tail` обычным; через `Printer`
  построчно — ровно наоборот.

### Открыто, Medium и ниже

Полный список — в отчёте, разделы «Medium and low-severity findings».
Стоит знать про эти, они про сами ворота:

- **M6.** `check_entry_points.dart` не ловит выброшенный `export`
  (проверено мутацией: убрать `export 'src/extensions/has.dart'` — ворота
  говорят «closed»). Ловят это `dart analyze` и `entry_point_*_test.dart`,
  но такой тест есть у 4 входов из 5.
- **M7.** `generate.dart` не видит незарегистрированную зону
  `BEGIN`/`END`: девятый файл с маркерами останется неперезаписанным, и
  `git diff --exit-code` будет зелёным.
- **M8.** Порог покрытия определяется генерируемым файлом: 89.6% всех
  непокрытых строк лежат в `style_colors.dart`. Комментарий в
  `dart.yml:95` несёт устаревшие 97.5%/76.0% против измеренных
  97.0%/77.3%.
- **M13/L12 и своими глазами.** Стражи времени чувствительны к загрузке
  машины. За прошлую волну один прогон дал `+632 -1` под нагрузкой от
  фаззинга, три следующих подряд — чистые; воспроизвести не удалось, и
  какой именно страж упал — не поймано. Это **второе** независимое
  наблюдение после отчёта. На общих runner'ах CI это может краснеть при
  исправном пакете.

## Что делать дальше

1. **Разбирать открытые находки.** То, что дорожало после публикации,
   уже сделано (`H9`). Из оставшихся раньше прочих стоят **H4** и
   **H6** — это потеря пользовательских данных; **H7** и **H8** требуют
   решения владельца о том, каким поведение должно быть, а не только
   правки.
2. Каждая находка идёт обычным заходом по `AGENTS.md`: спека → план →
   реализация → финальное ревью ветки → пуш и зелёный CI → мерж.
3. Публикация 4.0.0 — **после** разбора. Версию бампать не нужно, в
   `pubspec.yaml` уже 4.0.0.

## Чего не переоткрывать

**Закрытые пункты бэклога** — здесь, чтобы их не завели заново;
подробности в названных записях.

| что закрыто | запись |
|---|---|
| пункты первой бэклог-волны | `2026-08-05[11]` |
| сдвинутый `lastState` у `prepare` синка; два имени об одной повинности из двух | `2026-08-11[1]` |
| четвёртая поверхность незавершённой последовательности — вставка теряла текст | `2026-08-11[3]` |
| 7-битные `DCS`, `SOS`, `PM`, `APC` как открыватели управляющих строк | `2026-08-11[5]` |
| 8-битные C1 — **отклонено**, см. ниже | `2026-08-12[1]` |
| прогретый обход отвечал иначе свежего | `2026-08-13[1]` |
| состояние и ссылка на шве в конце входа; страж серии вставок; связывание набора открывателей; дартдок `insertAfter` | `2026-08-13[3]` |
| инвариант обратной сборки для незавершённых кодов | `2026-08-13[6]` |
| `Match` → `Piece`, `Matches` → `Pieces`, `parser.pieces`; моста-`typedef` нет намеренно | `2026-08-13[8]`, `[9]` |

**8-битные C1 escape-кодами не считаются, и распознавание их отклонено —
не отложено.** Ответ дан владельцем 2026-08-12, обоснование и источники —
`docs/records/2026-08-12[1]`. Коротко: пакет разбирает **декодированные
Dart-строки**, а не байтовые потоки; настоящий 8-битный C1
UTF-8-декодирования не переживает; эмиттеры по умолчанию пишут 7-битные
формы. Позиция закреплена тестами (`test/eight_bit_c1_test.dart`) и
проверена мутацией. Формулировку «пакет не распознаёт» легко принять за
описание дефекта — именно так этот пункт однажды и завели. Не чинить.

**Открытый срез не устанавливает коды, стоящие на разрезе.** Замысел
февраля 2025; `skipSet: true` в `substring` стоит намеренно, и
`ansi_constants_test` пинует это. Убирать его целиком — ошибка, её уже
делали. Чинилась только выжившая половина парного сброса (`22;1`), и
только у пары bold/dim: у `24`/`25`/`54`/`75` смена вида — чистая
установка, и она по-прежнему не пишется. Выглядит как тот же дефект, но
им не является; тест об этом есть.

**Канал ссылки лежит рядом с `State`, а не внутри.** Обоснование —
`docs/records/2026-08-07[1]`, раздел «Почему не внутри State».

**Инвариант `substring` — «где придержаны оба, открытие пришло первым»**;
напрашивающийся «придержано не больше одного» ложен
(`docs/records/2026-08-08[1]`).

**Дальше в `substring` копать нечего по скорости.** Потолок измерен
2026-08-08.

## Как здесь принято работать

Заход по бэклогу, имена записей, конвенции коммитов, список ворот и то,
чего не делать без слова владельца, — в `AGENTS.md`. Здесь не
повторяется.

Одно правило стоит прочесть до плана: **ожидаемые значения снимаются
пробником с живого кода**, а не выводятся рассуждением. За прошлую волну
оно сработало **шесть раз**, и все шесть раз неправ был не код:

- `Uri.encodeFull` не идемпотентен — экранирует `%`, и `a%20b` становится
  `a%2520b`. Спековый вариант ломал бы обычные адреса.
- В const-конструкторе нельзя ни вызвать функцию, ни спросить
  `contains`: выбранный вариант с `assert` оказался нереализуем.
- `_terminatedOpening` **годится** для всех четырёх форм незавершённого
  кода, хотя отчёт утверждал обратное: `ST` — это `ESC \`, и его `ESC`
  обрывает ждущую последовательность.
- Предложенное отчётом «не удалять `SGR` за незавершённым кодом» **не
  работает**: у оборванного `CSI` там стоит текст, а не `SGR`.
- Первый отпечаток поведения `Stack` не воспроизводился между запусками —
  в поверхность попал идентичностный `hashCode`. Как эталон он был бы
  бесполезен.
- `skipSet` в `substring` оказался не опиской, а замыслом; прямолинейное
  удаление уронило тест, и правильной оказалась гораздо более узкая
  правка.

К нему три правила, купленных прошлыми волнами:

- **Число без корпуса непроверяемо.** Либо корпус лежит рядом с числом,
  либо число объявлено относительным.
- **Тест, который не видели красным, — не тест.** Каждый страж прошлой
  волны проверен мутацией: возврат старого охранника даёт 28 падений в
  `reemission_invariant_test`, списочный `Stack` — отношение 5.37 при
  пороге 3.5, `skipSet` над выжившей — `22` вместо `22;1`.
- **Список открытого сверяется с отчётом, а не с прошлым handoff'ом.**
  H4 выпал именно так: волна закрывала находки по одной, и при
  переписывании handoff'а незакрытая пропала из обоих списков. Отчёт
  ревью — единственная полная опись; handoff пересчитывается по ней.
- **Сплошная замена слова портит то, ради чего замена делалась.** В
  волне H9 `perl` по слову `Match` прошёл и по новому стражу — файлу,
  который обязан писать `dart:core.Match`, — и обессмыслил его; поймано
  чтением диффа, в дартдок стража вписано, почему он говорит `Match`.
  Рядом уцелели только потому, что были названы заранее: `_MatchingState`
  (в нём `RegExpMatch` движка регулярок) и вся проза про regex. А метки
  сценариев бенчмарков (`'Memory / matches'`) оставлены уже по ходу:
  `benchmark/compare.dart` вычитывает старые ревизии из git и
  **сопоставляет прогоны по имени сценария** — переименование тихо
  разорвало бы сравнение с `perf-baseline-4.0.0`. Прежде чем менять имя
  везде, стоит спросить, где это имя — ключ, а где просто слово.

## Чем пакет занимался последние дни

| волна | спека / план | merge |
|---|---|---|
| … прежние волны … | см. прошлые handoff'ы в `docs/records/` | |
| Прогретый обход отвечает как свежий | `2026-08-13[1]`, `[2]` | `0f0080b` |
| Закрытие находок перед публикацией | `2026-08-13[3]`, `[4]` | `206a937` |
| **Независимое ревью в семь заходов** | `2026-08-13[5]` | `d0723e0` |
| **Разбор находок ревью** | `2026-08-13[6]`, handoff `[7]` | `d0723e0` |
| **`Match` → `Piece` (H9)** | `2026-08-13[8]`, `[9]` | `f1a927d` |

## Чему верить в этом документе

Написан на `main` @ `f1a927d`, по зелёным локальным воротам, по зелёному
CI на обеих ногах SDK (`a7978a4`, прогон `31712886635`) и по своим же
измерениям; H4 перепроверен пробником на влитом коде. Привычку прошлых
верификаций стоит держать: **доверять коду, тестам и git, а не итогам
чужих сессий, включая этот документ.**
Если что-то здесь расходится с репозиторием — прав репозиторий.
