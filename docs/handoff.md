# Handoff — состояние пакета и что делать дальше

ansi_escape_codes, 2026-08-15. Волна verification guards реализована и
разблокирована; ждёт ревью всей ветки, feature CI и мержа.

**Коротко:** M6, M7, M8 и кластер M13/L12 реализованы в отдельном worktree на
ветке `fix/verification-guards`. Блокер прошлого захода — `check_entry_points`
не замечал ошибку анализатора в экспортируемом `lib/src/...`-файле — закрыт
коммитом `88c7e73` после того, как владелец разрешил второй fix-раунд
(2026-08-15). Полные локальные ворота зелёные. Ветка **не отправлялась**, CI не
запускался, merge в `main` не делался: сначала ревью всех семи задач плана.

**Где что лежит:** `AGENTS.md` — правила; этот файл — текущая точка входа;
`docs/records/2026-08-15[4]-verification-guards-design.md` — спека волны;
`docs/records/2026-08-15[6]-verification-guards-plan.md` — её план;
`docs/records/2026-08-15[5]-pre-verification-guards-handoff.md` — архив
закрытого H8; `docs/architecture.md` — устройство кода; `docs/backlog.md` —
список владельца, агенты в него не пишут.

## Точная точка рестарта

- Рабочее дерево: `.worktrees/fix-verification-guards`, ветка
  `fix/verification-guards`, дерево чистое. HEAD — `88c7e73`.
- Локальный `main` — `623d1c6` (коммит плана), `origin/main` — `c6eda18`.
  **`main` на один коммит впереди remote**: план ещё не отправлен.
- Реализационные commits: M6 `5e4238c`, M7 `1ddb072`, M8 `53d9c36`,
  anchors `2c53313`, complexity `08ffedd`, lint `95c3a4d`, документация
  `2f278a3`/`293cd35`, правка путей и прямого entry-point fixture `c2d6c3c`,
  закрытие блокера `88c7e73`.
- **`docs/handoff.md` на `main` устарел** — он писался до реализации и говорит,
  что волна не начата. Актуален только этот файл в ветке; на `main` он приедет
  мержем, отдельно его править не надо.
- В рабочем дереве есть неотслеживаемый `.DS_Store`. Это не часть волны и не
  агентская правка: не добавлять, не удалять и не включать в commit без нового
  решения владельца.

## Ворота, снятые на `88c7e73`

Прогон последовательный, на одной машине, без параллельной нагрузки:

| ворота | результат |
|---|---|
| `dart format` | 150 файлов, 0 изменений |
| `dart analyze --fatal-infos` | 0 замечаний |
| `check_entry_points.dart` | 5 точек входа, 1648 публичных имён, замкнуты (2.9 s) |
| `generate.dart` + `git diff` | 8 зон, `lib/` не изменился |
| `dart test` | 903 теста зелёные |
| `memory_guard.dart` | 262.1 байта на match (полоса 159…332) |
| `complexity_guard.dart` | parse 2.00 (<2.5), slice 1.96 (<2.5), stack 2.27 (<3.5), insert 34.26 (>24.0) |
| `dart doc --dry-run` | 0 warnings, 0 errors |
| `dart pub publish --dry-run` | 0 warnings, архив 265 KB |

Это **не** заменяет feature CI: нижняя нога матрицы, SDK `3.6.0`, локально не
проверяется вообще.

## Как закрыт блокер (`88c7e73`)

Прошлый заход проверял диагностику только в `resolved.units` — это сам entry
point и его части. Экспортируемая библиотека — отдельный unit, поэтому ошибка
в `lib/src/extensions/has.dart` не была видна инструменту **вообще**: RED
воспроизведён как exit 0 на приватной константе с неопределённым именем.
Модель элементов у сломанной библиотеки всё равно выдаётся, только меньшая, —
и `--update-snapshot` записал бы усечённый namespace как истину.

Теперь `_analysesWithoutError` перед обоими оракулами рекурсивно читает все
`*.dart` под `lib/`. **Фильтр по severity `ERROR` намеренный:** analyzer API
отдаёт ещё и `DEPRECATED_MEMBER_USE` уровня INFO (депрекация `RegExp` и
`RegExpMatch` новее SDK-floor'а), которых `dart analyze --fatal-infos` не
показывает, — свип без фильтра красил бы пакет, который все прочие ворота
зовут чистым. Предупреждения и линты модель не портят и остаются делом
`dart analyze`. Цена: чекер 2.0 s → 2.9 s.

## Выбранная волна: verification guards

### M6 — public namespace snapshot

`check_entry_points.dart` сохраняет прежний closure-walk публичных сигнатур и
получает второй, независимый oracle: точный JSON snapshot имён, экспортируемых
каждой из пяти точек входа. Обычный запуск ловит missing, unexpected и
несовпадение самого множества `lib/*.dart`; явный `--update-snapshot` нужен
только для принятого API-diff и не используется в CI. Оба оракула теперь
работают только после чистого свипа ошибок анализатора.

На живой базе namespace counts: `ansi.dart` 510, зонтичный entry point 1037,
`extensions.dart` 8, `style.dart` 91, `utils.dart` 2. Мутация удаления
`has.dart` export должна красить специальное ворото и называть
`StringHasEscapeCodesExtension`.

### M7 — generator registry preflight

Генератор сначала сравнивает registry восьми известных путей с рекурсивно
обнаруженными marker-файлами в `lib/`. Лишняя, отсутствующая, повторная либо
непарная marker-зона завершает процесс до первой записи. Успешный запуск
пишет и сообщает ровно восемь зон; повторный запуск идемпотентен.

### M8 — coverage рукописного кода

Workflow сохраняет полный `lcov.info` для artifact и строит отдельный
`lcov.gated.info` с `--ignore-files=**/style_colors.dart`. Новый floor —
95.0% и применяется только ко второму файлу. Живой probe дал 79.646%
(`2387/2997`) полного и 95.976% (`2385/2485`) filtered coverage. Нулевой
знаменатель остаётся красным.

### M13/L12 — complexity отдельно от общего test process

Детерминированные semantic anchors остаются в обычном тестовом корпусе:
parse, slicing, Stack и insert доказывают точный результат и объём работы.
Wall-clock ratios уехали в отдельный `benchmark/complexity_guard.dart`:
stable-only, прогрев, попарно чередуемые small/large замеры, медиана пар,
достаточно большие batches и калибровка на живом коде с обратными мутациями.
Он не объединяется с cold `memory_guard.dart`.

Пороговые значения, corpus sizes и counts выбраны пробником и зафиксированы в
`benchmark/complexity_guard.dart`; их не следует менять, чтобы замаскировать
красную мутацию.

## Следующий безопасный шаг

1. Ревью всех семи задач плана `[6]` по всей ветке. Замеры wall-clock в
   параллельных агентах недостоверны: complexity guard перезапускать
   последовательно и отдельно.
2. Починить реальные находки ревью, повторить локальные ворота.
3. `git push -u origin fix/verification-guards`, дождаться зелёной матрицы
   `3.6.0`/`stable`.
4. Только после зелёного CI: `git merge --no-ff` в `main`, `git push origin
   main` (он же увезёт неотправленный `623d1c6`), дождаться main CI, затем
   заархивировать этот handoff в `docs/records/` и переписать `docs/handoff.md`.

## Чего не делать

- Не публиковать 4.0.0, не ставить тег и не бампать версию.
- Не менять публичный API, `lib/`, SDK-floor `^3.6.0` или marker text этой
  волной.
- Не брать другие Medium/Low находки и не заводить пункты в
  `docs/backlog.md`.
- Не мержить в `main` до зелёного feature CI на обеих ногах матрицы.
- Не расширять свип ошибок анализатора на warning и info: это осознанное
  решение, а не недосмотр.
- Не стирать или добавлять `.DS_Store` без воли владельца.

## Последние волны

| волна | спека / план / архив | merge |
|---|---|---|
| Terminal и stacked reset (H7) | `2026-08-14[7]`, `[8]`, `[10]` | `bbbb210` |
| Межстрочный cursor save (H8) | `2026-08-15[1]`, `[2]`, `[3]` | `2db4abd` |
| **Verification guards (M6, M7, M8, M13/L12)** | спека `[4]`, план `[6]`; ветка `fix/verification-guards` @ `88c7e73` | ждёт ревью и CI |

## Чему верить в этом документе

Ворота в таблице выше сняты на `88c7e73` фактическим прогоном, а не выведены
рассуждением. Про CI, ревью и merge здесь ничего не утверждается: их ещё не
было. `.superpowers/.../progress.md` — внутренний ledger исполнения плана, а
не источник состояния. Если handoff расходится с кодом, тестами или git,
правы они.
