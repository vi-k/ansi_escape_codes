# Handoff — состояние пакета и что делать дальше

ansi_escape_codes, 2026-08-09. Передача контекста для новой сессии.

**Коротко:** весь бэклог корректности закрыт, 4.0.0 готова и **не
опубликована** — публикация ждёт решения владельца. Открытого кода нет;
открыты три записанных пункта в `docs/backlog.md` и одно решение.

**Где что лежит:** `AGENTS.md` в корне — правила работы над пакетом (их
читают оба инструмента: Claude Code через `CLAUDE.md`, Codex напрямую);
этот файл — состояние пакета; `docs/architecture.md` — как устроен код;
`docs/backlog.md` — открытые пункты;
`docs/records/` — записи волн (спеки, планы, отчёты, прошлые handoff'ы),
у каждой в шапке сказано, что с ней стало.

## Состояние репозитория

- `main` @ `57b8443`, запушен, дерево чистое.
- Версия **4.0.0**, на pub.dev — 3.1.2. Публикация и тег `v4.0.0` не
  делались: владелец сказал «пока не публикуем» 2026-08-05 и решения не
  менял. Ни одна волна с тех пор версию не трогала — все правки
  неизданные, поэтому CHANGELOG правится на месте, а не breaking-нотами.
- Ворота на HEAD зелёные: `dart format --set-exit-if-changed`,
  `dart analyze --fatal-infos`, `dart run tool/check_entry_points.dart`,
  **512 тестов**, `dart run benchmark/memory_guard.dart` (265.2 при
  полосе 159…332), `dart doc --dry-run` 0/0, примеры,
  `dart pub publish --dry-run` 0 предупреждений.
- CI зелёный на ubuntu x64: последний прогон — на `1a02d6c`.

## Что делать дальше

### 1. Публикация 4.0.0 — единственное, что ждёт слова владельца

Пакет к ней готов и был готов с 5 августа. Когда прозвучит «публикуем»:

```bash
dart format --output=none --set-exit-if-changed .
dart analyze --fatal-infos
dart run tool/check_entry_points.dart
dart test
dart run benchmark/memory_guard.dart
dart doc --dry-run
dart pub publish --dry-run   # ожидается 0 warnings
dart pub publish
git tag v4.0.0 && git push origin v4.0.0
```

Версию бампать не нужно — в `pubspec.yaml` уже 4.0.0.

### 2. `insertAfter` — четвёртая поверхность дефекта, который волна закрыла на трёх

Самый содержательный пункт бэклога, и единственный, который требует
дизайна, а не правки. Полное описание с репродукциями — в
`docs/backlog.md`; здесь суть, чтобы решать, брать ли:

```dart
Parser('aa\x1B]0;title').insertAfter(2, 'X')
// 'aa\x1B]0;titleX' — плоский текст всё ещё 'aa', а надо 'aaX'
```

Вставка, нацеленная на самый конец текста, где строка кончается **внутри**
незавершённой последовательности, уходит внутрь неё. Условие точное и
проверено исчерпывающе (7196 пар «вход × позиция»): вход кончается внутри
незавершённого `OSC` **и** `pos == length`. `insertBefore` цел на любой
позиции — он встаёт перед кодами шва.

Почему это не сделали заодно: починка означает, что вставка **дописывает
байты ко входу, который сегодня копирует байт в байт**, — а это смена
контракта, и решать её владельцу. Плюс две ловушки, которые делают
очевидное решение неочевидным: `ST` лечит только `OSC` (тот же шов глотает
вставку за голым `ESC` и за оборванным `CSI`), а `ST` после **открытия
ссылки** положит вставку внутрь ссылки, а не за неё.

Брать — через brainstorming → спека → план → subagent-driven, как две
последние волны.

### 3. Две мелочи в `docs/backlog.md`

- `_SinkPrinterBase.prepare` возвращает на место обе ссылочные переменные
  и долг терминатора, но `lastState` оставляет сдвинутым: спросить у синка
  про кусок со стилем — значит покрасить этим стилем следующий `write`.
  Наследство волны `docs/records/2026-08-05[10]`, репро — в
  `docs/backlog.md`.
- `_closesLinkAtEnd` и параметр `closeLink` распоряжаются двумя
  повинностями, названы по одной. Обе приватные — переименование
  поведенчески нейтрально, потому бэклог, а не дефект.

## Как здесь принято работать

Заход по бэклогу, имена записей, конвенции коммитов, список ворот и то,
чего не делать без слова владельца, — в `AGENTS.md`. Здесь это не
повторяется: правило, записанное дважды, расходится.

Одно из тех правил стоит того, чтобы новая сессия прочла его до плана:
**ожидаемые значения снимаются пробником с живого кода**, а не выводятся
рассуждением. За две волны они дважды оказались неверны даже так;
реализаторы это ловили и не подгоняли тесты под код.

## Чем пакет занимался последние дни

Хронология волн, все влиты в `main`:

| волна | спека / план | merge |
|---|---|---|
| Ревью проекта, 15 пунктов | `2026-08-04[1]` | 6 заходов |
| Независимая верификация | `2026-08-05[9]` | — |
| Фиксы по верификации | `2026-08-05[10]` | `56c8244` |
| Предпубликационный свип | — | — |
| Бэклог-волна | `2026-08-05[11]` | `7a7a550` |
| Непрерывность ссылки (N5) | `2026-08-07[1]`, `[2]` | `5623e12` |
| Оптимизация `substring` | — | `1a02d6c` |
| Терминация `OSC` | `2026-08-08[1]`, `2026-08-09[1]` | `57b8443` |

Две вещи из них стоит знать, прежде чем что-то менять в парсере:

**Канал ссылки лежит рядом с `State`, а не внутри.** Обоснование — в
`docs/records/2026-08-07[1]`, раздел «Почему не внутри State»: `SGR 0`
ссылку не закрывает, она не вкладывается, а её байты несут терминатор и
`id=`, которые спека `OSC 8` делает значимыми именно для ссылок,
разорванных переносом строки. Не переоткрывать этот вопрос без чтения
раздела.

**Инвариант `substring` — «где придержаны оба, открытие пришло первым»,
и это второй инвариант, а не первый.** Первый — «в любой момент
придержано не больше одного» — написан в `docs/records/2026-08-08[1]`
вместе с причиной, по которой он ложен, и коммитом замены (`de1b5bf`). Он
напрашивается сильнее верного; не реализовывать его заново.

**Дальше в `substring` копать нечего по скорости.** Потолок измерен
2026-08-08: вся ссылочная бухгалтерия текстовой ветки стоит ~1,2%, а
оставшийся ~1% живёт вне её — это более крупный `Match` и то, что вокруг
него делает куча. Методика замера: обе стороны из `git archive` со своим
`pub get`, 28 процессов на сторону, **парное** сравнение по раундам —
межпроцессный шум 4–8,7% больше сигнала, так что критерий «полосы не
перекрываются» здесь не различает ничего.

## Чему верить в этом документе

Написан после мержа `57b8443`, по зелёным воротам и по своим же
измерениям. Но следующей сессии стоит держать привычку прошлой
верификации: **доверять коду, тестам и git, а не итогам чужих сессий,
включая этот документ.** Если что-то здесь расходится с репозиторием —
прав репозиторий.
