name: ansi_escape_codes
description: A toolkit for working with ANSI escape codes and analyzing strings containing them.
version: 2.2.0
homepage: https://github.com/vi-k/ansi_escape_codes

environment:
  sdk: ^3.7.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
